// DEPRECATED — was causing page reload loops on Vercel.
// No-op service worker. The browser may keep this active for a while
// after it's no longer registered, so this does absolutely nothing.
// The app has no offline mode, so PWA support was unnecessary.
